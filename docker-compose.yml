services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: voice_demo
      POSTGRES_PASSWORD: voice_demo
      POSTGRES_DB: voice_demo
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
